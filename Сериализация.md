Данная библиотека собрала в себе наработки для быстрой сериализации объектов. 
		Со временем данная библиотека будет дополнятся новыми методами. 
        
        
      	
      	//Подключение! 
      	
         Для подключения библиотеки нужно создать макет с типом ""Двоичные данные"" 
		и загрузить туда обработку. 
      	 
      	
        Чтобы получить доступ к библиотеке в модуле объекта нужно (желательно, но необязательно) прописать
		шаблон подключения:
      	1 инструкция   
        ////////////////////////////////////////////////////////////////////// 
		Функция ПолучитьБиблиотеку(ИмяМакета) Экспорт //<<
	    
		Библиотека = ПолучитьМакет(ИмяМакета);
		
		АдресХранилища = ПоместитьВоВременноеХранилище(Библиотека);
		
		ИмяФайла = ПолучитьИмяВременногоФайла("epf");
		
		Библиотека.Записать(ИмяФайла);
		
		ОписаниеЗащитыОтОпасныхДействий = Новый ОписаниеЗащитыОтОпасныхДействий;
		ОписаниеЗащитыОтОпасныхДействий.ПредупреждатьОбОпасныхДействиях = Ложь;
		
		Обработка = ВнешниеОбработки.Создать(ИмяФайла,Ложь,ОписаниеЗащитыОтОпасныхДействий);
		
		Возврат Обработка;
		
		КонецФункции //<<
        ////////////////////////////////////////////////////////////////////// 
		
		Далее будет перечисление методов, которые доступны в данной библиотеке.  
		
		//Документация:   
		
		
		-->> Серверные (для обычных форм тоже) методы реализованные в модуле объекта библиотеки:
		
      	Методы: 
		
		>> Функция ПолучитьТабличныйДокумент(АдресВременногоХранилища,ФорматФайла) Экспорт //в параметры передается: 1. Передается адрес временного хранилища; 
		//2. Формат файла (строка). Возвращает табличный документ
		
		>> Функция ТаблицуЗначенийВТабличныйДокумент(ТаблицаЗначений,КоординатаКолонки = 1,КоординатаСтроки = 1,ОбвестиЯчейки = Ложь) Экспорт //в параметры передается: 
		1.таблица значений; 
		2.Координата колонки - (необязательно) - Число; 3. Координата строки - (необязательно) - Число; 
		4. Обвести ячейки - (необязательно) - булево. Возвращает табличный документ
		
		>> Функция ТабДокВТаблицуЗначений(ТабДок,КоличествоКолонок = 0) Экспорт //в параметры передается: 1.Табличный документ, 2.КоличествоКолонок - тип - Число 
		//(необязательно) (по умолчнаию = 0) количество отбираемых колонок из табличного документа. Возвращает таблицу значений
		      
		>> Функция ТабДокВТаблицуЗначенийСОтборомКолонок(ТабДок,ОтборКолонок) Экспорт //в параметры переадется: 1. ТабДок - тип - ТабличныйДокумент, 
		//2. ОтборКолонок - тип - Массив (передается массив именами колонока (тип - строка))
		
		>> Функция ТаблицуЗначенийВМассивСтруктур(Таблица) Экспорт //в параметры передается таблица значений. Возвращает массив структур
		
		>> Функция ДеревоЗначенийВТаблицуЗначений(Знач ДеревоЗначений) Экспорт //в параметры передается: ДеревоЗначений - тип - ДеревоЗначений. Данная функция создает таблицу значений, 
		//заполняет таблицу из дерева и возвращает таблицу значений.
		
		>> Функция ПолучитьПутьКФормеКлиентУправляемая() Экспорт //возвращает  путь к форме ""КлиентУправляемая""
      	
      	>> Функция ПолучитьПутьКФормеКлиентОбычный() Экспорт //возвращает  путь к форме ""КлиентОбычный""
		
		>> Функция ПолучитьМассивКолонокТаблицы(Таблица) Экспорт // в параметры передается Таблица - тип - ТаблицаЗначений. Возвращает массив имен колонок таблицы.
		
		>> Функция СтрокуТаблицыВСтруктуру(СтрокаТаблицы,Таблица) Экспорт // в параметры передается: СтрокаТаблица - тип - (СтрокаТаблицыЗначений или ДанныеФормыЭлементКоллекции),
		//Таблица - тип - (ТаблицаЗначений или ДанныеФормыКоллекция). Возвращает структуру.
		
		>> Функция JSONВСтруктуру(json) Экспорт // в параметры передается jsob - тип - строка (формат json)
		 
		>> Функция СтруктуруВJSON(Структура) Экспорт // в параметры передается: Структура - тип - Структура. Данная функция преобразовывает структуру в JSON и возвращает json строку.
		 
		>> Процедура ЗаполнитьТаблицуИзМассиваСтруктур(Таблица,МассивСтруктур) Экспорт // в параметры передается: Таблица - тип - (ТаблицаЗначений или ДанныеФормыКоллекция),МассивСтруктур - тип - Массив.
		//Данная процеудра заполняет таблицу значений данными из массива структур. Аналог Таблица.Загрузить(ТаблицаЗначений), только заполняет данные из структуру. 
		//(Имена колонок должны совпадать с именами ключей структур!)
		
		
		
		-->> Клиентские методы реализованные для управляемых форм в модуле формы ""КлиентУправляемая"":
		 
		Чтобы получить к методам формы ""КлиентУправляемая"" библиотеки, нужно прописать (только для управляемой формы):
		2 инструкция
      	////////////////////////////////////////////////////////////////////// 
		Первым делом для получения доступа к форме надо выполнить код из первой инструкции и получить возвращаемое значение (Обработка). Далее в серверной функции обращаемся                 
		к функции ""ПолучитьПутьКФормеКлиентУправляемая()"", она возвратит путь к форме!
        ////////////////////////////////////////////////////////////////////// 
      	
      	 Методы:
		 
		>> Функция ПолучитьТабличныйДокументНаКлиенте(ПутьКФайлу) Экспорт //в параметры передается: ПутьКФайлу - тип - Строка. Возвращает табличный документ.
		 
		>> Функция ПрочитатьJSONФайл(ПутьКФайлу) Экспорт //в параметры передается: ПутьКФайлу - тип - Строка. Возвращает json строку. 
		
		
		
		-->> Клиентские методы реализованные для обынчных форм в модуле формы ""КлиентОбычная"":
      	3 инструкция
        ////////////////////////////////////////////////////////////////////// 
      	 Для доступа к форме ""КлиентОбычный"" такая же инструкция как и для управляемой формы. Надо получить обработку и обратиться к функции                                                               
		""ПолучитьПутьКФормеКлиентОбычный()"", она возвратит путь к форме! 
      	////////////////////////////////////////////////////////////////////// 
      	
      	 Методы:
		
		>> Функция ПолучитьТабличныйДокументНаКлиенте(ПутьКФайлу) Экспорт //в параметры передается: ПутьКФайлу - тип - Строка. Возвращает табличный документ.
		
		>> Функция ПрочитатьJSONФайл(ПутьКФайлу) Экспорт //в параметры передается: ПутьКФайлу - тип - Строка. Возвращает json строку.
