Данная библиотека собрала в себе наработки для быстрой разработки ПО в системе 1С. 
		Со временем данная библиотека будет дополнятся новыми методами. 
        
        
      	
      	 //Подключение! 
      	
         Для подключения библиотеки нужно создать макет с типом ""Двоичные данные"" 
		и загрузить туда обработку. 
      	 
      	
        Чтобы получить доступ к библиотеке в модуле объекта нужно (желательно, но необязательно) прописать
		шаблон подключения:
      	1 инструкция   
        ////////////////////////////////////////////////////////////////////// 
		Функция ПолучитьБиблиотеку(ИмяМакета) Экспорт //<<
	    
		Библиотека = ПолучитьМакет(ИмяМакета);
		
		АдресХранилища = ПоместитьВоВременноеХранилище(Библиотека);
		
		ИмяФайла = ПолучитьИмяВременногоФайла("epf"); 
		
		Библиотека.Записать(ИмяФайла);
		
		ОписаниеЗащитыОтОпасныхДействий = Новый ОписаниеЗащитыОтОпасныхДействий;
		ОписаниеЗащитыОтОпасныхДействий.ПредупреждатьОбОпасныхДействиях = Ложь;
		
		Обработка = ВнешниеОбработки.Создать(ИмяФайла,Ложь,ОписаниеЗащитыОтОпасныхДействий);
		
		Возврат Обработка;
		
		КонецФункции //<<
        ////////////////////////////////////////////////////////////////////// 
		
		Далее будет перечисление методов, которые доступны в данной библиотеке.    
		
		//Документация:   
		
		
		-->> Серверные (для обычных форм тоже) методы реализованные в модуле объекта библиотеки:
		
      	 Методы:  
      	
		>> Функция ПолучитьДокументацию() Экспорт // возвращает документацию по библиотеке
		
		>> Функция ОбъединитьЗначенияСтрокиТабличнойЧастиВОдну(СтрокаТабличнойЧасти,МассивКолонокТабличнойЧасти)  Экспорт  // в параметры передается два значения:
		//1. Строка табличной 
		//части; 2. Массив колонок табличной части возвращает конкатенацию строк строки табличной части
		
		>> Функция ПолучитьХЭШСтроки(Знач Строка) Экспорт //в параметры передается строка любой длинны и возвращается хэш строки в формате sha512
		
		>> Процедура СократитьСтроку(Значение) Экспорт // в параметры передается строка, убирает все лишние символы и склеивает строку воедино
		
		>> Функция ПолучитьФорматФайла(ПутьКФайлу) Экспорт //в параметры передается ""путь к файлу"" в формате строка и возвращает формат файла
		
		>> Функция ЭтоРежимКонфигуратора() Экспорт // данная функция проверяет, запущен ли текущий пользователь под конфигуратором. 
		//Возвращает булево значение. 
		
		>> Процедура Сон(ВремяВСекундах = 1) Экспорт //в параметр ВремяВСекундах значение типа ""Число"" 
		//(значение по умолчанию -> 1 (секунда)). Данная процеудра предназначена для того, чтобы произвести паузу в программе.  
		
		>> Процедура УбратьНевалидныеСимволыВСтроке(Строка) Экспорт // в параметры передается строка. Данный метод очищает символы, которые невалидные для кода.
		
		>> Процедура РеверсМассива(Массив) Экспорт // в параметры передается массив. Данная процедура переварачивает данные в обратную сторону
		
		>> Функция СтрРазделить_(Строка,Разделитель = "",ВключатьПустые = Истина) Экспорт // в параметры передается: Строка (обязательно), Разделитель (по умолчанию = "") (необязательно)
		//,ВключатьПустые - тип - Булево (необязательно) (по умолчанию = Истина). Возвращает массив символов.
      	
      	>> Функция ПолучитьПутьКФормеКлиентУправляемая() Экспорт //возвращает  путь к форме ""КлиентУправляемая""
      	
      	>> Функция ПолучитьПутьКФормеКлиентОбычный() Экспорт //возвращает  путь к форме ""КлиентОбычный""
		
		>> Функция ВычислитьРазмерЗначенияВБитах(Значение) Экспорт //возвращает размер значения в битах  
		
		>> Функция ЗначениеВДвоичноеПредставление(Знач Значение) Экспорт // возвращает значение в двоичном (бинарном) представлении    
		
		>> Функция SizeOf(Символ) Экспорт // возвращает размер символа в байтах 
		
		
		
		-->> Клиентские методы реализованные для управляемых форм в модуле формы ""КлиентУправляемая"":
		 
		Чтобы получить к методам формы ""КлиентУправляемая"" библиотеки, нужно прописать (только для управляемой формы):
		2 инструкция
      	////////////////////////////////////////////////////////////////////// 
		Первым делом для получения доступа к форме надо выполнить код из первой инструкции и получить возвращаемое значение (Обработка). Далее в серверной функции обращаемся                 
		к функции ""ПолучитьПутьКФормеКлиентУправляемая()"", она возвратит путь к форме!
        ////////////////////////////////////////////////////////////////////// 
      	
      	 Методы:
      	
		>> Функция ФормаВыбораФайла(Режим = Неопределено) Экспорт //в параметры передаеся значение типа - 
		РежимДиалогаВыбораФайла(Открытие,Сохранение,ВыборКаталога), по умолчанию значение - РежимДиалогаВыбораФайла.Открытие . Возвращает путь к выбранному файлу или                         
		 каталогу. 
		
		>> Функция ВычислитьРазмерЗначенияВБитах(Значение) Экспорт //возвращает размер значения в битах  
		
		>> Функция ЗначениеВДвоичноеПредставление(Знач Значение) Экспорт // возвращает значение в двоичном (бинарном) представлении    
		
		>> Функция SizeOf(Символ) Экспорт // возвращает размер символа в байтах
		                                                                                                  
		>> Процедура РеверсМассива(Массив) Экспорт // в параметры передается массив. Данная процедура переварачивает данные в обратную сторону
		
		>> Процедура Сон(ВремяВСекундах = 1) Экспорт //Аналогичен процедуре из модуля объекта (см. выше!) 
		
		
		
		-->> Клиентские методы реализованные для обынчных форм в модуле формы ""КлиентОбычный"":
      	3 инструкция
        ////////////////////////////////////////////////////////////////////// 
      	 Для доступа к форме ""КлиентОбычный"" такая же инструкция как и для управляемой формы. Надо получить обработку и обратиться к функции                                                               
		""ПолучитьПутьКФормеКлиентОбычный()"", она возвратит путь к форме! 
      	////////////////////////////////////////////////////////////////////// 
      	
      	 Методы:
      	
		>> Функция ФормаВыбораФайла(Режим = Неопределено) Экспорт  //в параметры передаеся значение типа - 
		//РежимДиалогаВыбораФайла(Открытие,Сохранение,ВыборКаталога), по умолчанию значение - РежимДиалогаВыбораФайла.Открытие. 
		//Возвращает путь к выбранному файлу или каталогу.   
		
		>> Функция ВычислитьРазмерЗначенияВБитах(Значение) Экспорт //возвращает размер значения в битах  
		
		>> Функция ЗначениеВДвоичноеПредставление(Знач Значение) Экспорт // возвращает значение в двоичном (бинарном) представлении    
		
		>> Функция SizeOf(Символ) Экспорт // возвращает размер символа в байтах   
		
		>> Процедура РеверсМассива(Массив) Экспорт // в параметры передается массив. Данная процедура переварачивает данные в обратную сторону
		
		>> Процедура Сон(ВремяВСекундах = 1) Экспорт //Аналогичен процедуре из модуля объекта (см. выше!)
